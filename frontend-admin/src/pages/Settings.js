import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Settings = () => {
  return (
    <div>
      <Title level={2}>Settings</Title>
      <p>Settings interface for backend configuration, API keys, and system settings will be implemented here.</p>
    </div>
  );
};

export default Settings;