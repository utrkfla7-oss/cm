import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ImportManager = () => {
  return (
    <div>
      <Title level={2}>Import Manager</Title>
      <p>Import manager interface for batch importing from IMDb/TMDb will be implemented here.</p>
    </div>
  );
};

export default ImportManager;